{% extends 'base.html.twig' %}

{% block title %}New User
{% endblock %}

{% block body %}
	<h1>Inscrire</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn">{{ button_label|default('Soumettre') }}</button>
	{{ form_end(form) }}

	<a href="{{ path('app_login') }}">back to list</a>
{% endblock %}

